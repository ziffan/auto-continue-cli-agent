# CONTEXT.md — status proyek

> Update **tiap sesi**. Baca ini dulu sebelum kerja — jangan asumsikan status.
> **Baru di repo ini?** Doc ini = **jurnal status per-sesi** (catatan internal, padat jargon `ADR-`/`G-`/`I-`).
> Untuk memahami proyeknya, mulai dari [`../README.md`](../README.md) lalu [`PROJECT.md`](PROJECT.md) — bukan dari sini.

---

## Status saat ini

- **Terakhir diupdate:** 2026-07-21 (sesi **Windows, dgn owner** — **remediasi G-61 di mesin Windows + sanity pasca-rilis publik**). Bukan sesi kode — **nol perubahan `src/`**; docs + verifikasi.
  **Step 0 — divergensi besar & keputusannya.** `git fetch` melaporkan `origin/main` **forced-update** + **133 ahead / 143 behind** (artefak murni rewrite history G-61: semua sha berubah → merge-base mundur ke commit purba, bukan tanda kerja lokal hilang). CONTEXT/ISSUES mengintruksikan "**hapus & clone-ulang** repo di Windows". **Instruksi itu di-challenge dan diganti** — clone-ulang akan menghilangkan file gitignored yang tak ada di remote (`.internal/` 3 file, `COWORK-TOOLING-NOTES.md`, `.claude/settings.local.json`) + memaksa rebuild native + membuat **path absolut action Task Scheduler** menunjuk folder mati. **Pra-syarat diverifikasi dulu, bukan diasumsikan:** tree bersih · stash kosong · keempat commit lokal teratas ditemukan di `origin/main` dgn sha baru (`c77f4ae`→`afa6043`, `5c410b3`→`d82918b`, `76ffbba`→`23c27e3`, `882452f`→`f88ab0a`) → **nol kerja lokal unik**. Eksekusi: `reset --hard origin/main` + `reflog expire --expire=now --all` + `gc --prune=now`. **Verifikasi setara-clone:** `git log --format='%ae|%ce' --all|sort -u` = nol email pribadi (hanya noreply) DAN `git cat-file -t c77f4ae` → `fatal: Not a valid object name` (objek terhapus **fisik**). **Aksi wajib G-61 untuk mesin Windows: TUNTAS** → **G-63** baru. Diluruskan juga: objek unreachable tak pernah ter-push, jadi urgensinya = kebersihan `.git` lokal, bukan pencegahan kebocoran publik.
  **Sanity mesin Windows pasca-rilis (semua hijau).** `npm install` → `up to date` (nol paket → **nol rebuild native**, jadi risiko `.node` terkunci EBUSY saat sesi CC hidup tak muncul) · `npm run build` sukses · **`npm run check` = 707 pass + 2 skip / 54 file**. **Selisih vs CI-windows dijelaskan tepat, bukan diabaikan:** CI 701+2 (run `29728711038`, commit `702afbc`) **+1** `CONTRIBUTING.md` (`a28107d`) **+5** file gitignored yang ada di mesin ini (`.claude/settings.local.json`, `COWORK-TOOLING-NOTES.md`, `.internal/`×3) = **707** — persis perilaku bergantung-mesin D-5/RD-5 (`AGENTS.md` symlink tetap terhitung krn gate pakai `statSync`).
  **Autostart Task Scheduler diverifikasi utuh** (ADR-026): `State=Running` · action `conhost.exe --headless` (G-52) · `MultipleInstances=IgnoreNew` + trigger @logon ber-`Repetition PT1M` (G-49) · `ExecutionTimeLimit=PT0S` · principal `ziffa`/Interactive/**RunLevel=Limited** (bukan SYSTEM — larangan keras DECISIONS) · proses nyata pid 12824 `dist\cli\index.js daemon` + heartbeat 0s. **`LastTaskResult=0x800710E0`** (`net helpmsg 4320` = "operator/administrator refused") **BUKAN error** → watchdog menolak instance baru saat daemon hidup = bukti ia bekerja → **G-64** baru.
  **Keamanan menjalankan gate saat sesi CC lain hidup — diverifikasi sebelum dijalankan, bukan diasumsikan.** Sesi `#5dev` (CC, `D:\PROYEK\LexSolva-INSOLVENSI\lexsolva-mvp`) RUNNING selama sesi ini. Dicek: (a) nol test menyentuh pipe daemon produksi — tiap test IPC memakai pipe acak (`acca-test-*`/`acca-rearm-*`/`acca-inject-*`), `\\.\pipe\acca-daemon` (`paths.ts:55`) tak pernah dipakai; dua file yang menyebutnya = gate artefak baca-template; (b) **14 file test yang membuka DB, SEMUANYA** override `ACCA_DATA_DIR` ke temp — nol pelanggar. Pasca seluruh rangkaian: `#5dev` tetap RUNNING/alive. **Daemon TIDAK di-restart** (sengaja): commit 20–21 Jul nol menyentuh `src/`, jadi kode di memori identik perilakunya; restart ditunda sampai sesi lexsolva selesai.
  **G-65 (kejujuran metodologis):** `npm install --dry-run` dipilih agar nol mutasi, **tapi tetap menjalankan lifecycle `prepare`** → `tsc` benar-benar jalan & `dist/` ditulis ulang. Klaim "tidak memutasi apa pun" dikoreksi ke user saat itu juga. Efeknya kebetulan menguntungkan (langkah build tersubstitusi + membuktikan sesi lexsolva tak terganggu), tapi itu keberuntungan bukan desain — nol efek samping sesungguhnya butuh `--ignore-scripts`.
  **Docs sesi ini:** GOTCHAS (**G-63/G-64/G-65** + 3 baris indeks), `.archived/GOTCHAS-full.md` (3 writeup), CONTEXT (ini). **Kode: nol.** ISSUES/DECISIONS/CHANGELOG **sengaja tak disentuh** — nol issue dibuka/ditutup, nol keputusan struktural, nol perubahan user-facing.
  **Next konkret (sesi berikutnya):** tak ada milestone aktif. (1) **F-6** (dedup `emit-on-change`) = P3 paling aman/kecil, cocok subagent Sonnet; (2) **F-5** (efisiensi `stripAnsi`+regex per-chunk) — hati-hati, menyentuh jalur detektor, argumen "risiko sentuh > nilai P3" yang menahan F-4 berlaku sebagian; (3) sisa tak berubah: W-3 favicon residual · B-3 · I-15 (limit asli) · I-33-residual. **M-remote** menunggu owner. Catatan ops: daemon masih memuat `dist` pra-rebuild hari ini (aman, perilaku identik) — restart saat ada perubahan `src/` nyata & sesi lain tak berjalan.

- **Terakhir diupdate:** 2026-07-21 (sesi **Linux, dgn owner** — **RILIS PUBLIK v0.1.0 — P-1 DITUTUP**). Step 0: sinkron `origin/main` (0/0), tree bersih, branch `claude/session-start-phjitd`. Bukan sesi kode — verifikasi kesiapan publik + eksekusi langkah rilis owner-driven.
  **Konteks:** owner sudah flip repo ke publik sebelum sesi; tugas = cek kondisi vs checklist P-1 lalu selesaikan sisanya.
  **(1) Verifikasi repo publik (via GitHub MCP API):** `private:false`/`visibility:public` ✅ · Apache-2.0 terdeteksi ✅ · description + 5 topics ✅ · CI `main` (`f30e915`) success (semua 8 run hijau) ✅. **G-61 dicek ULANG di remote (bukan cuma lokal):** 182 commit remote (2 halaman `list_commits`) = **100% noreply**, nol non-noreply → nol email pribadi terekspos di kondisi publik.
  **(2) Sisa P-1 saat cek awal:** tag/Release `v0.1.0` **belum ada** (`list_tags`+`list_releases` kosong) · PVR tak bisa diverifikasi via MCP (field `security_and_analysis` tak diekspos) → owner konfirmasi manual sudah aktif.
  **(3) Tag v0.1.0 — dikerjakan owner-driven (G-62 baru):** agent buat annotated tag lokal (tagger di-set identitas owner `ziffan@users.noreply.github.com`, bukan "Claude", demi konsistensi pasca-G-61) TAPI **`git push origin v0.1.0` ditolak 403** oleh egress proxy (push di-scope ke branch designated saja; ref tag bukan branch) + GitHub MCP tak punya `create_release` → **jalur agent buntu, bukan diakali**. Owner publish Release via **GitHub UI (Cara A)**. Diverifikasi via `get_release_by_tag`: `draft:false, prerelease:false`, target `f30e915`, author `ziffan`, id `357080209`, URL `/releases/tag/v0.1.0`.
  **(4) Dua kosmetik tersisa juga diselesaikan owner:** website About = `https://ziffany.firdinal.my.id/` ✅ · tab Issues dimatikan (`has_issues:false`, konsisten posture demo-only CONTRIBUTING) ✅.
  **Hasil: P-1 TUNTAS PENUH — acca resmi rilis publik v0.1.0.** Tak ada blocker rilis tersisa.
  **Docs sesi ini:** ISSUES (P-1 ✅ + G-62 inline), GOTCHAS (**G-62** + indeks), CHANGELOG (`[0.1.0] - 2026-07-21`), CONTEXT (ini). Kode: **nol** (sesi verifikasi/rilis).
  **Next konkret (sesi berikutnya):** tak ada milestone aktif — sisa **P3 oportunistik** tak berubah (W-3 favicon residual · F-4/5/6 · B-3 · I-15 live-verify limit asli · I-33-residual). **M-remote** menunggu owner buka lagi. **⚠️ Ingat G-61:** clone di mesin Windows masih history lama → hapus & clone-ulang sebelum kerja di sana (`git pull` biasa menghidupkan commit ber-email lama).

- **Terakhir diupdate:** 2026-07-20 (sesi **Linux, dgn owner** — **rapih-rapih dokumentasi pra-publik**). Step 0: sinkron `origin/main` (0/0), tree bersih, di branch `claude/new-session-sw1q2i`. Bukan sesi kode — persiapan berkas komunitas + metadata + sapuan akhir. **Tahapan yang dijalankan (usulan 4-tahap, disetujui owner):**
  **(Tahap 0 — sapuan akhir kerahasiaan, verifikasi):** metadata commit author/committer diperiksa manual (`git log --all --format='%ae %ce'|sort -u`) → **100% noreply** (`noreply@anthropic.com` co-author + `ziffan@users.noreply.github.com` + `noreply@github.com`); nol email domain-pribadi. Ini jalur yang **tak terjangkau gate file-based** (G-61) — satu-satunya cara = cek manual metadata. Nol file rahasia tracked; path personal cuma fixture yang memang mencolok (`/home/ziffan/secret-project` dll).
  **(Tahap 1 — metadata `package.json`):** temuan = `repository`/`bugs`/`keywords` semua `undefined`. Tambah `repository` (URL git kanonik `github.com/ziffan/auto-continue-cli-agent`) + `keywords`. **`bugs` sengaja dikosongkan** (kontribusi tertutup → tak ada public bug tracker; keamanan lewat PVR SECURITY.md; URL `/issues` yang salah lebih buruk drpd tak ada). `"private": true` tetap.
  **(Tahap 2 — berkas komunitas, keputusan owner = TERTUTUP/demo-only):** `CONTRIBUTING.md` baru — repo = artefak demonstrasi solo/non-komersial, **PR eksternal tak diterima rutin** (fork & pakai-ulang Apache 2.0 dipersilakan; keamanan → SECURITY.md; tracker internal = docs/ISSUES.md, bukan GitHub Issues). **Tanpa** CODE_OF_CONDUCT / issue-PR template (sesuai posture tertutup).
  **(Tahap 3 — keterbacaan docs, keputusan owner = reading-guide ringan):** 1-baris pointer di header `CONTEXT.md` & `ISSUES.md` ("baru di repo ini? mulai dari README/PROJECT — ini jurnal/tracker internal"). Additive, nol hard-delete, nol ubah isi. Semua link relatif file yang disentuh diverifikasi resolve.
  **Verifikasi:** `npm install` (prepare→build sukses, 0 vuln) + `npm run check` HIJAU **704 pass / 54 file** (703 CI + 1 = file baru CONTRIBUTING ikut ter-enumerasi gate working-tree, persis perilaku bergantung-mesin D-5). Gate `no-canonical-limit-literals` + `ci-workflow` (email) lolos atas file baru.
  **Tahap 4 = tetap tangan owner (P-1, tak berubah):** flip visibility → aktifkan PVR → tag `v0.1.0`+Release; set GitHub About (description+topics+website); **⚠️ hapus & clone-ulang repo di mesin Windows** sebelum kerja berikutnya (G-61 — `git pull` biasa menghidupkan commit ber-email lama). `.claude/skills/*` tetap sengaja publik.

- **Terakhir diupdate:** 2026-07-20 (sesi **Linux, dgn owner** — **lisensi Apache 2.0 (ADR-029) + kesiapan repo publik**). Step 0: sinkron dgn `origin/main` (0/0), tree bersih. Bukan sesi milestone — tak ada milestone aktif; ini kerja kesiapan rilis.
  **(1) Lisensi di-LOCK → ADR-029 (Accepted, owner Ziffany Firdinal).** Menutup Pending "Lisensi repo" yang menganggur sejak M0. Premis lama ("terkait rencana komersialisasi") **gugur** — owner menyatakan acca non-komersial; yang diinginkan = atribusi ke lab payung **kampusmerah.com** + author `ziffany.firdinal.my.id`. **Challenge dijalankan:** AGPL-3.0 + dual-license ditawarkan eksplisit (owner pemegang copyright tunggal → bisa jual lisensi komersial terpisah) → **ditolak owner**. **Apache 2.0 > MIT** krn patent grant §3 + klausa merek §6 + **§4(d): NOTICE wajib ikut terdistribusi** = atribusi melekat pada lisensi, bukan cuma README. Realisasi: `LICENSE` (teks kanonik apache.org **verbatim**, sha256 `cfc7749b…`, 202 baris — **di-`cp`, tidak digenerate model**; hanya placeholder appendix diisi) · `NOTICE` · `package.json` (`license`/`author`/`homepage`; **`"private": true` DIPERTAHANKAN**) · README `## Lisensi & dukungan`. **Tanpa header copyright per-file** (appendix = rekomendasi; 100+ file = noise, nol nilai hukum tambahan utk pemegang hak cipta tunggal).
  **(2) Audit kesiapan publik — NOL temuan kerahasiaan.** Dipindai working tree **+ seluruh git history** (186 file, `.git` 6.8 MB): nol secret (semua hit `sk-ant-…`/`ya29.` = fixture test palsu + 1 baris prosa ADR yang menjelaskan ruleset redaksi), nol email, nol UUID sesi nyata, nol data usage akun. Satu-satunya path personal = string fixture `'/home/ziffan/secret-project'` (disengaja mencolok). `.gitignore` sudah menutup `.env`/`*.db`/`data/`/`.internal/`.
  **(3) Kesiapan publik dibangun (subagent Sonnet, di-review Opus):** `.github/workflows/ci.yml` (npm ci + `npm run check`, matrix `ubuntu-latest`+`windows-latest`, Node dari `.nvmrc`, **action dipin SHA 40-hex** bukan tag mutable, `permissions: contents:read`, concurrency cancel-in-progress) · `test/ci-workflow.test.ts` (gate artefak **kelas I-34**: pinning SHA, matrix dua-OS, `npm run check` terpanggil, least-privilege, SECURITY.md nol email) · `SECURITY.md` (disclosure privat — bukan issue publik; model kepercayaan; batasan diketahui-dan-diterima: single-user R-5/R-7, DACL named pipe Windows terbuka by design ADR-023/G-41, `acca web` tanpa auth by design ADR-028) · `engines.node` `">=24 <25"`→`">=24"` · README `## English summary` (+ **disclaimer non-afiliasi Anthropic/Google**) + `## Metodologi doc-first` (repo = artefak demonstrasi kampusmerah.com; `.claude/skills/*` sengaja publik).
  **(4) DUA koreksi Tier-1 atas draf subagent → G-60.** `SECURITY.md` versi subagent memuat klaim yang **bertentangan dgn kode/ADR**: (a) `~/.gemini/oauth_creds.json` — padahal `credentials.ts` hanya baca `~/.claude/.credentials.json`; (b) allowlist egress memuat `cloudcode-pa.googleapis.com` — padahal `http.ts` **sengaja mengeluarkannya** (ADR-018 di-supersede ADR-019). Ketahuan lewat `grep` ke kode, bukan lewat membaca dokumen. Link advisory relatif → absolut. Subagent sendiri melaporkan 1 bug regex gate (`- uses:` tak tertangkap → lolos-palsu; tertangkap oleh guard anti-kosong).
  **Verifikasi:** `npm run check` lokal hijau **703 pass / 54 file**; **CI GitHub Actions HIJAU percobaan pertama** (run `29726998571`): `ubuntu-latest` 56s **703 pass** · `windows-latest` 1m47s **700 pass + 2 skip** (skip = `ipc-stale-socket.test.ts`, POSIX-only — persis perilaku bergantung-mesin yang didokumentasikan D-5/RD-5). **Angka-of-record kini = CI**, bukan pengukuran manual.
  **Koreksi status lama:** catatan 19 Jul "instal Windows baru diuji Linux" **sudah tak berlaku** — owner konfirmasi acca jalan di PC Windows-nya (20 Jul).
  **Commit sesi ini:** `b9983e0` (`chore(public-prep)`, sudah push) + commit session-end ini. **Repo MASIH PRIVATE** — owner: "belum publik sampai benar-benar pas".
  **(5) SAPUAN PRIVASI LANJUTAN + REWRITE HISTORY (atas permintaan owner, pasca-session-end).** Owner minta pengecekan data pribadi/serial-number perangkat. Hasil: **nol** temuan hardware (serial/BIOS/MAC/machine-id/MachineGuid/SID/product-key/output `systeminfo`-`dmidecode` ter-paste), **nol** file biner tracked, **nol** IP server/jalur SSH/akun hosting (tak ada yang nyasar dari repo kampusmerah.com), **nol** ID sesi/transcript nyata. **TAPI ditemukan 1 BOCOR yang audit sebelumnya lewatkan: 25/178 commit membawa alamat email pribadi owner (di domain sendiri, bukan bentuk noreply) pada metadata author+committer** — jalur PII yang `git grep` (baca blob) maupun gate "SECURITY.md nol email" tak pernah sentuh → **G-61**. **Remediasi (keputusan owner: rewrite sekarang, mumpung private+solo):** `git filter-repo --mailmap` → seluruh history kini `ziffan@users.noreply.github.com`; `git config user.email` mesin ini di-set ke noreply. **Verifikasi:** tree hash HEAD lama `c57cbac3…` == baru (identik → hanya metadata berubah, nol perubahan isi), commit count 178==178, backup bundle pra-rewrite disimpan di scratchpad sesi. **SEMUA SHA BERUBAH** (`2d89abc`→`b9983e0`, `e856722`→`a4cc97d`, `9a4a541`→`d6d323c`, `c77f4ae`→`afa6043`); referensi SHA di docs sudah diperbarui.
  **(6) Kebocoran-ulang nyaris terjadi & gate pencegahnya (ditangkap OWNER, bukan agent).** Saat menulis dokumentasi kebocoran di (5), alamat email itu ditulis **verbatim** ke `docs/CONTEXT.md` + `docs/.archived/GOTCHAS-full.md` — menghapusnya dari metadata lalu menanamnya sebagai teks. Owner yang menangkapnya; belum ter-push, jadi cukup redaksi + `git commit --amend` (verifikasi: nol kemunculan di seluruh 179 commit). **Gate baru** di `test/ci-workflow.test.ts` (test ke-7): nol alamat email di domain pribadi owner pada file tracked — **domain sebagai URL situs tetap boleh** (atribusi ADR-029), sebagai alamat email tidak. **Negative-control dijalankan:** file umpan → gate MERAH + menyebut file pelakunya; dibuang → hijau. Batas gate ditulis eksplisit di komentarnya: **metadata commit TAK terjangkau gate file-based** → cek manual `git log --all --format='%ae'|sort -u` (dan `%ce`).
  **Force-push (pengecualian sadar):** aturan repo melarang force-push ke `main`; di sini sah & disetujui owner — solo, pra-publik, nol kolaborator. `--force-with-lease`, `9a4a541…702afbc`. **CI hijau di atas history yang ditulis ulang** (run `29728711038`): ubuntu **703 pass**, windows **701 pass + 2 skip**. Verifikasi sisi remote via `gh api` (bukan hanya lokal): metadata commit di GitHub = noreply semua.
  **Catatan jujur:** GitHub bisa menyimpan commit tak-terjangkau sampai GC-nya jalan (diakses bila SHA lama diketahui). Repo tak pernah publik & SHA lama hanya ada di transcript sesi → risiko mendekati nol; bila ingin kepastian penuh sebelum flip visibility, minta GitHub Support jalankan GC.
  **⚠️ AKSI WAJIB DI MESIN WINDOWS:** clone di sana masih memegang history lama. **`git pull` biasa akan menarik balik commit ber-email lama** → **hapus clone lama & `git clone` ulang** sebelum kerja berikutnya di Windows (lihat G-61).
  **Next konkret (sesi berikutnya):** **P-1** = 3 langkah owner berurutan (aktifkan private vulnerability reporting → flip visibility → tag `v0.1.0` dari commit ber-CI-hijau; **bukan** `npm publish`). Sisa P3 tak berubah: W-3 residual (favicon) · F-4/5/6 · B-3 · I-15 · I-33-residual. **W-2 DITOLAK owner.** M-remote masih ditunda.

- **Terakhir diupdate:** 2026-07-19 (sesi **Windows, dgn owner** — **W-1: security-review gate M-web DIJALANKAN → LULUS PENUH → milestone M-web DITUTUP FORMAL**). Step 0: lokal tertinggal 5 commit dari `origin/main` (ops/packaging sesi Linux) → fast-forward bersih. Angka-of-record diperbarui **691 pass + 2 skip** (diukur Windows 19 Jul; skip POSIX bergantung-mesin, D-5). Bukan sesi kode — gate verifikasi.
  **Gate M-web (skill `milestone-wrapup`, persona security-review vs THREAT-MODEL §9.3 T-W1..T-W6):** verifikasi INDEPENDEN (baca code + jalankan test, bukan baca komentar). **Semua CONFIRMED, nol gap:**
  **(T-W1 info-disclosure)** `SessionStatusView` (`sessions.ts:225-249`) tak punya `cli_session_id`/`cwd`/field-audit; caller `web.ts:44` proyeksikan via `toSessionStatusView` SEBELUM ke `buildStatusPayload` (tipe `SessionStatusView[]` menegakkan boundary); usage lewat `formatUsageLines` (kind/bar/pct saja, G-9), events lewat `SUMMARY_ALLOWLIST` (`evidence` mentah tak pernah keluar). Negative-control test (`web.test.ts`): JSON kabel tak memuat `RESUME-CAP-SECRET-ID`/`RAHASIA`/`LEAK-ME`/`SECRET-model`.
  **(T-W2)** GET-only → 405+`Allow: GET`. **(T-W3)** Host-guard 403, `isLoopbackHost` IPv6-bracket-aware + tolak `attacker.localhost.evil.com`/kosong/undefined. **(T-W4)** page self-contained — grep nol `http(s)://`/`src=`/`href=`/`@import`; satu-satunya fetch same-origin `/api/status`. **(T-W5)** `textContent`/`createElement`, nol `innerHTML`. **(T-W6)** `resolveWebPort` validasi 1..65535, bind gagal → pesan jelas + `exit 1`; `acca web` proses terpisah dari daemon (crash web ≠ ganggu auto-resume). **R-7** (loopback terjangkau proses lokal lain) DITERIMA — single-user desktop, sejajar R-5.
  **Verifikasi:** `npx vitest run test/web.test.ts test/cli-dispatch.test.ts` (22 hijau) + `npm run check` full **691 pass + 2 skip** (gate `no-canonical-limit-literals` hijau — frasa limit di integration-test = fixture exempt). **Tak ada kode diubah** (gate murni verifikasi + docs).
  **Aktivitas kedua sesi ini — PELANGSINGAN read-set session-start ronde 2 (`76ffbba`):** hot-path ritual **~1098→~500 baris (~54%)**, nol hard-delete. **Lever A (arsipkan stale):** CONTEXT 80→27 (sisakan 2 entri 19-Jul; 6 entri 18-Jul + 2 dupe audit → arsip) · ISSUES 139→72 (tabel indeks-tertutup redundan → peta-grup + pointer; bagian Terbuka utuh; W-1 writeup ke `.archived/ISSUES-closed.md`). **Lever B (owner MEMBALIK aturan 18-Jul "jangan sentuh spec" — spec fair-game utk proyek matang):** PROJECT 370→232 (§4 Flow + §5 Wireframe → `.archived/PROJECT-design.md`) + **skill `session-start` diubah**: PROJECT+ARCHITECTURE turun dari wajib-baca-penuh ke **referensi on-demand** (skim header; deep-read hanya saat task menyentuh spec/arsitektur). Gate `no-canonical-limit-literals` hijau (116 test, +1 file arsip).
  **Aktivitas ketiga sesi ini — W-3 polish page (SEBAGIAN, Tier-2):** kolom `reset_at`/`updated_at` tabel sesi kini human-readable — `page.ts` `fmtTs` (epoch-ms → `HH:MM` lokal, sisip nama hari bila >24 jam, anti-B-2). **Browser-side, nol field baru ke `/api/status`** (jaga T-W1 nol-jalur-data-baru; `reset_at`/`updated_at` sudah ada di `SessionStatusView`). Testability tanpa jsdom/duplikasi: `FMT_TS_JS` diekspor sbg string → di-embed ke halaman DAN dievaluasi di test via `new Function` (5 test: 4 behavioral + 1 embed-guard; eslint no-implied-eval di-disable ber-rasional utk konstanta sendiri). **Runtime-verified:** `acca web` + curl → HTML terkirim memuat `fmtTs`, 0 href/src eksternal (T-W4 utuh). **Favicon DITUNDA** (butuh `href="data:"` → memecah assertion blanket T-W4; nilai rendah). Angka-of-record: **697 pass + 2 skip**.
  **Docs sesi ini:** MILESTONES (M-web ✅ DITUTUP FORMAL), ISSUES (W-1 ✅ + indeks diringkas + W-3 sebagian), CHANGELOG (gate lulus + W-3), CLAUDE.md §2 (status+angka), CONTEXT (ini), skill session-start (ritual on-demand), DECISIONS (Change Log ritual), + 3 file `.archived/`. Kode: `src/web/page.ts` + `test/web.test.ts`. Memori `feedback-lean-session-context` diperbarui.
  **Next konkret (sesi berikutnya):** tak ada milestone aktif — semua sisa P3 oportunistik: **W-2** (`acca daemon --web` co-host, opsional — perhatikan erosi isolasi T-W6) · **W-3 residual** (favicon inline — perlu sempitkan regex test T-W4) · F-4/5/6 · B-3 · I-15 (live-verify limit asli) · I-33-residual. M-remote menunggu owner membuka lagi. Opsional non-P3: uji metode instal lintas-mesin di Windows sungguhan (baru diuji Linux, G-59).

- **Terakhir diupdate:** 2026-07-19 (sesi **Linux, dgn owner** — ops: **update daemon terinstall + `npm link` + packaging instalasi lintas-mesin**). Angka-of-record tak berubah (689 pass, gate hijau di mesin ini — lihat CLAUDE.md §2). Bukan sesi milestone — **W-1 (security-review gate M-web) diusulkan di session-start tapi owner alihkan ke kebutuhan ops mendesak**; W-1 TETAP terbuka, belum disentuh 2 sesi berturut.
  **(1) Update daemon terinstall (`71f6993`):** `dist/` yang jalan sebagai systemd `--user` service ketinggalan 5 commit (banner/Web UI/`acca prune`/fix G-58) — `npm run build` + `systemctl --user restart acca-daemon` + verifikasi (journalctl bersih, heartbeat PID baru, tak ada sesi RUNNING/LIMIT_HIT terdampak). README ditambah section **"Update acca (setelah ada commit baru)"** (Linux systemd + Windows Task Scheduler) supaya owner bisa mandiri.
  **(2) `npm link`** — `acca` kini di PATH global di laptop ini (sebelumnya harus `node dist/cli/index.js`).
  **(3) Packaging instalasi lintas-mesin (`59b4beb`→`bf2c71e`→`02a047b`):** owner tanya cara paket-install cepat di mesin lain. Kandidat jelas `npm install -g git+https://…` **diuji langsung, TERBUKTI TAK ANDAL** (bug npm — skrip `prepare` dijalankan dua kali oleh `arborist/rebuild.js`, invokasi kedua tanpa `node_modules/.bin` → `tsc: not found`; direproduksi 2× termasuk setelah `typescript` dipindah ke `dependencies`) — **G-59 baru**. Metode yang dipakai & teruji end-to-end dari clone segar (2×, `/tmp`): `git clone` manual + `npm install` (kini auto-build via script `prepare` baru) + `npm link`. README section baru **"Mesin baru, belum ada clone"** (prasyarat: repo private → `gh auth login`/SSH; toolchain native build utk `node-pty`/`better-sqlite3`) + peringatan eksplisit jangan pakai shortcut `git+`.
  **Verifikasi:** `npm run check` (689 pass) + gate `no-canonical-limit-literals` (111) hijau pasca semua edit. Commit sesi ini (**4, semua sudah push**): `71f6993` (readme update-daemon) → `59b4beb` (script prepare) → `bf2c71e` (typescript→dependencies) → `02a047b` (readme install lintas-mesin).
  **Docs:** README (2 section baru), `package.json` (`prepare` + `typescript` dependency), CHANGELOG (Changed), GOTCHAS (**G-59** + indeks), CONTEXT (ini).
  **Next konkret (sesi berikutnya):** (1) **W-1: security-review gate M-web** — masih prioritas tertunda tertinggi, 2 sesi berturut dialihkan ke kerja lain; (2) opsional: uji metode instal baru di mesin Windows sungguhan (baru diuji di Linux); (3) sisa P3 lama tak berubah (W-2/W-3/F-4..6/B-3/I-15/I-33-residual).

---

> **Riwayat sesi lebih lama** (M1 s.d. 17 Jul + entri 18 Jul sebelumnya) diarsip ke [`.archived/CONTEXT-2026-07.md`](.archived/CONTEXT-2026-07.md) — greppable, di luar jalur session-start.
